{
	"name": "sweeper",
	"version": "1.0.1",
	"description": "Clean up messy markdown with a diff preview",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "vitest run",
		"test:watch": "vitest",
		"cli": "npx tsx src/cli.ts"
	},
	"keywords": [],
	"author": "schovi",
	"license": "MIT",
	"devDependencies": {
		"@types/diff": "^7.0.2",
		"@types/node": "^16.11.6",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.17.3",
		"obsidian": "latest",
		"tslib": "^2.8.1",
		"typescript": "4.7.4",
		"vitest": "^1.6.1"
	},
	"dependencies": {
		"diff": "^7.0.0"
	}
}
